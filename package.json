{
    "name": "code-collab-hub",
    "private": true,
    "workspaces": [
        "frontend"
    ],
    "scripts": {
        "dev": "concurrently -n \"FE,BE\" -c \"cyan,green\" \"npm run dev -w frontend\" \"cd backend && make dev\"",
        "install:all": "npm install",
        "test": "npm test --workspaces --if-present"
    },
    "devDependencies": {
        "concurrently": "^8.2.2",
        "jsdom": "^24.0.0"
    }
}